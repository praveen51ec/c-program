#include <stdio.h>
#include<string.h>
int main(void)
{
	int i,n;
	char a[10],rev[10];
	scanf("%s",a);
	n=strlen(s);
	for(i=n;i>=0;i--)
{
	static x=0;
	rev[x]=a[i];
	x++;
	printf("%c",a[i]);
}
	return 0;
}
