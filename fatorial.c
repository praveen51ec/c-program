#include <stdio.h>
#include<string.h>
int main(void)
{
	int i,b;
	char a[10],rev[10];
	scanf("%s",s);
	n=strlen(s);
	for(i=b;i>=0;i--)
{
	static x=0;
	rev[x]=a[i];
	x++;
	printf("%c",a[i]);
}
	return 0;
}
